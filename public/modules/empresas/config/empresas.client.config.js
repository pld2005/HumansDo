'use strict';

/*/ Configuring the Articles module
angular.module('empresas').run(['Menus',
	function(Menus) {
		// Set top bar menu items
		//Menus.addMenuItem('topbar', 'Empresas', 'empresas', 'dropdown', '/empresas(/create)?',true,['superadmin'],1);

	}
]);*/